[project]
name = "rutext-gen"
version = "0.1.0"
description = "Приложение дял генерации текста и анализа"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "beautifulsoup4>=4.15.0",
    "fake-useragent>=2.2.0",
    "numpy>=2.5.1",
]

[project.scripts]
project = "main:main"
